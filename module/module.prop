id=ech-wk
name=ECH-Workers
version=${{ github.event.inputs.module_version }}  # 对应你输入的版本号
versionCode=${{ github.run_number }}  # 自动递增的构建编号
author=byJoey (Auto-Built via GitHub Actions)
description=ECH 加密传输代理（支持 KSU WebUI 配置管理）
webuiIcon=webroot/icon.png
