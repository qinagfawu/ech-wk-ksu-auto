# ech-wk-ksu-auto
ECH-Workers KSU 模块，通过 GitHub Actions 自动构建。

## 使用方法
1. 进入 Actions → 选择 `Build & Release ECH-Workers KSU Module` → 点击 `Run workflow`
2. 输入版本号，触发构建
3. 构建完成后，在 Releases 页面下载模块 ZIP 包
4. 在 KernelSU 中安装模块，重启后即可使用
